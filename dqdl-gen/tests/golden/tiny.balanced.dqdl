# Generated by dqdl-gen from tiny.csv (csv).
# 12 rows profiled, 2 columns. Strictness: balanced.
# Rule types verified against AWS's DQDL parser on 2026-09-23: https://github.com/awslabs/dqdl
#
# A generated ruleset is a starting point derived from one profile of one file. Review it
# before committing it to a pipeline: the tool describes the data it was shown, which is not
# the same as the data you expect tomorrow.

Rules = [
    # 12 rows in the file, allowing 20% either way at balanced strictness
    RowCount between 9 and 15,

    # 2 columns in the profiled file
    ColumnCount = 2,

    # no nulls in 12 rows
    IsComplete "code",

    # all 12 values distinct and non-null, so this looks like a key
    IsPrimaryKey "code",

    # observed 0 to 11, widened by 5% at balanced strictness
    ColumnValues "code" between -1 and 12,

    # read as int64
    ColumnDataType "code" = "Long",

    # no nulls in 12 rows
    IsComplete "label",

    # observed lengths 1 to 1 characters, padded by 1 at balanced strictness
    ColumnLength "label" between 0 and 2
]
