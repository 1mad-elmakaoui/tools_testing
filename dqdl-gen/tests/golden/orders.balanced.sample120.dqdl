# Generated by dqdl-gen from orders.csv (csv).
# 120 rows profiled from a sample (file total unknown), 5 columns. Strictness: balanced.
# Rule types verified against AWS's DQDL parser on 2026-09-23: https://github.com/awslabs/dqdl
#
# A generated ruleset is a starting point derived from one profile of one file. Review it
# before committing it to a pipeline: the tool describes the data it was shown, which is not
# the same as the data you expect tomorrow.

Rules = [
    # 5 columns in the profiled file
    ColumnCount = 5,

    # no nulls in 120 rows; from a sample of 120 rows
    IsComplete "order_id",

    # all 120 values distinct and non-null, so this looks like a key; from a sample of 120
    # rows
    IsPrimaryKey "order_id",

    # observed 100000 to 100119, widened by 5% at balanced strictness; from a sample of 120
    # rows
    ColumnValues "order_id" between 99994 and 100125,

    # read as int64; from a sample of 120 rows
    ColumnDataType "order_id" = "Long",

    # no nulls in 120 rows; from a sample of 120 rows
    IsComplete "status",

    # 4 distinct values across 120 non-null rows: NEW (30), PAID (30), SHIPPED (30), VOID
    # (30); from a sample of 120 rows
    ColumnValues "status" in ["NEW", "PAID", "SHIPPED", "VOID"],

    # 4 distinct values observed; this guards against the set collapsing, while the
    # allowed-value rule guards against it growing; from a sample of 120 rows
    DistinctValuesCount "status" between 1 and 4,

    # observed lengths 3 to 7 characters, padded by 1 at balanced strictness; from a sample of
    # 120 rows
    ColumnLength "status" between 2 and 8,

    # no nulls in 120 rows; from a sample of 120 rows
    IsComplete "region",

    # 3 distinct values across 120 non-null rows: ap-south-1 (40), eu-west-1 (40), us-east-1
    # (40); from a sample of 120 rows
    ColumnValues "region" in ["ap-south-1", "eu-west-1", "us-east-1"],

    # 3 distinct values observed; this guards against the set collapsing, while the
    # allowed-value rule guards against it growing; from a sample of 120 rows
    DistinctValuesCount "region" between 1 and 3,

    # observed lengths 9 to 10 characters, padded by 1 at balanced strictness; from a sample
    # of 120 rows
    ColumnLength "region" between 8 and 11,

    # 118 of 120 rows non-null (0.983 observed), less a 0.01 margin at balanced strictness;
    # from a sample of 120 rows
    Completeness "amount" >= 0.97,

    # observed 12.5 to 838.5, widened by 5% at balanced strictness; from a sample of 120 rows
    ColumnValues "amount" between -28.8 and 879.8,

    # read as double; from a sample of 120 rows
    ColumnDataType "amount" = "Double",

    # no nulls in 120 rows; from a sample of 120 rows
    IsComplete "customer_note",

    # 12 distinct values across 120 non-null rows: note- (10), note-x (10), note-xx (10),
    # note-xxx (10), note-xxxx (10), and 7 more; from a sample of 120 rows
    ColumnValues "customer_note" in ["note-", "note-x", "note-xx", "note-xxx", "note-xxxx", "note-xxxxx", "note-xxxxxx", "note-xxxxxxx", "note-xxxxxxxx", "note-xxxxxxxxx", "note-xxxxxxxxxx", "note-xxxxxxxxxxx"],

    # 12 distinct values observed; this guards against the set collapsing, while the
    # allowed-value rule guards against it growing; from a sample of 120 rows
    DistinctValuesCount "customer_note" between 1 and 12,

    # observed lengths 5 to 16 characters, padded by 1 at balanced strictness; from a sample
    # of 120 rows
    ColumnLength "customer_note" between 4 and 17
]
